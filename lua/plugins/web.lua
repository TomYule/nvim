-- Web debugging (Chrome/Vue)
-- This config is outdated and relies on hardcoded paths.
-- Recommended to use `js-debug-adapter` via Mason/nvim-dap instead.
return {
  "microsoft/vscode-chrome-debug",
  enabled = false,
}
